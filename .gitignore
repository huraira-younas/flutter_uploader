# ── Secrets (never commit) ────────────────────────────────────────────────────
# Local secrets directory (``enviroment.json``, optional Drive JSON/tokens, etc.)
app/secrets/

# Google Drive OAuth (client secret + cached user token)
app/gdrive_token.json
app/gdrive.json
**/client_secret*.json
credentials.json

# App Store Connect API private keys
*.p8
AuthKey_*.p8
private_keys/

# ── Generated output ──────────────────────────────────────────────────────────
app/logs/
app/outputs/

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
.venv-*/
.venv*/
venv/
ENV/
.envrc
*.egg
*.egg-info/
.eggs/
dist/
build/
dist-installer/
pip-wheel-metadata/

# ── Tooling / tests ───────────────────────────────────────────────────────────
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/

# ── Optional local launcher scripts (repo root; same as `python3 run.py`) ─────
# /run

# ── OS & editors ──────────────────────────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp
*~

# ── Flutter / Dart (artifacts if copied into this tree) ───────────────────────
.dart_tool/
**/android/.gradle/

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store
